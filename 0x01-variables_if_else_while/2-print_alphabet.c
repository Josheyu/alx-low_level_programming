#include <stdio.h>
/**
*main -> assign a random number to the variable n each time it is executed
*based a condition
*Return: always 0
*/
int main(void)
{
char a;
for (a = 'a'; a <= 'z'; a++)
putchar(a);
//putchar('\n');
return (0);
}